import React from 'react';
import {View, Text} from "react-native";

const Page = () => {
    return (
        <View className="flex-1 justify-center items-center">
            <Text className="font-bold">Settings Page</Text>
        </View>
    );
};

export default Page;